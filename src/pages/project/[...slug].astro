---
import { getCollection, render } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  const projects = await getCollection('project');
  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project },
  }));
}

interface Props {
  project: CollectionEntry<'project'>;
}

const { project } = Astro.props;
const { Content } = await render(project);
---

<BaseLayout title={`${project.data.title} | Project`} description={project.data.description}>
  <article class="project">
    <header class="project-header">
      {project.data.thumbnail && (
        <div class="thumbnail">
          <img src={project.data.thumbnail} alt={project.data.title} />
        </div>
      )}

      <div class="header-meta">
        <span class={`type-badge type-${project.data.type}`}>
          {project.data.type}
        </span>
        <span class={`status-badge status-${project.data.status}`}>
          {project.data.status}
        </span>
        {project.data.featured && (
          <span class="featured-badge">Featured</span>
        )}
      </div>

      <h1>{project.data.title}</h1>

      <div class="project-meta">
        <time datetime={project.data.date.toISOString()}>
          {project.data.date.toLocaleDateString('en-US', {
            year: 'numeric',
            month: 'long'
          })}
        </time>

        {project.data.collaborators && project.data.collaborators.length > 0 && (
          <div class="collaborators">
            <strong>Collaborators:</strong>
            {project.data.collaborators.map((collaborator, index) => (
              <span>{collaborator}{index < project.data.collaborators.length - 1 ? ', ' : ''}</span>
            ))}
          </div>
        )}

        {project.data.methodology && project.data.methodology.length > 0 && (
          <div class="methodology">
            <strong>Methods:</strong>
            {project.data.methodology.map((method) => (
              <span class="method-tag">{method}</span>
            ))}
          </div>
        )}
      </div>

      {project.data.outputs && project.data.outputs.length > 0 && (
        <div class="outputs">
          {project.data.outputs.map((output) => (
            output.url ? (
              <a href={output.url} class="output-link" target="_blank" rel="noopener noreferrer">
                {output.label || output.type} →
              </a>
            ) : (
              <span class="output-label">{output.label || output.type}</span>
            )
          ))}
        </div>
      )}
    </header>

    <div class="project-content">
      <Content />
    </div>

    {project.data.relatedProcess && project.data.relatedProcess.length > 0 && (
      <aside class="related">
        <h3>Related Process Entries</h3>
        <ul>
          {project.data.relatedProcess.map((entry) => (
            <li><a href={`/process/${entry}`}>{entry}</a></li>
          ))}
        </ul>
      </aside>
    )}

    <nav class="project-nav">
      <a href="/project" class="back-link">← Back to Projects</a>
    </nav>
  </article>
</BaseLayout>

<style>
  .project {
    max-width: 800px;
    margin: 0 auto;
  }

  .project-header {
    margin-bottom: 3rem;
  }

  .thumbnail {
    width: 100%;
    max-height: 400px;
    overflow: hidden;
    border-radius: 0.75rem;
    margin-bottom: 2rem;
    background: var(--bg);
  }

  .thumbnail img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .header-meta {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .type-badge,
  .status-badge {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 0.25rem 0.625rem;
    border-radius: 0.25rem;
  }

  .type-badge {
    color: var(--accent);
    background: var(--accent-subtle);
    border: 1px solid color-mix(in srgb, var(--accent) 25%, transparent);
  }

  .status-badge {
    color: var(--text-tertiary);
    background: transparent;
    border: 1px solid var(--border);
  }

  .featured-badge {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    padding: 0.25rem 0.625rem;
    background: #97742f20;
    color: #97742f;
    border: 1px solid #97742f40;
    border-radius: 0.25rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .project-header h1 {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 2.5rem;
    line-height: 1.2;
    margin-bottom: 1.5rem;
  }

  .project-meta {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .collaborators,
  .methodology {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
  }

  .collaborators strong,
  .methodology strong {
    color: var(--text-primary);
  }

  .method-tag {
    font-size: 0.8125rem;
    padding: 0.125rem 0.5rem;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 0.25rem;
    font-family: var(--font-mono);
  }

  .outputs {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    padding: 1.5rem;
    background: var(--bg);
    border-radius: 0.5rem;
    margin-top: 1.5rem;
  }

  .output-link {
    color: var(--accent);
    text-decoration: none;
    font-size: 1rem;
    font-weight: 600;
    transition: color 0.2s ease;
  }

  .output-link:hover {
    color: var(--accent-hover);
    text-decoration: underline;
  }

  .output-label {
    color: var(--text-secondary);
    font-size: 1rem;
    font-weight: 500;
  }

  .project-content {
    font-size: 1.0625rem;
    line-height: 1.75;
    margin-bottom: 3rem;
  }

  .project-content :global(h2) {
    font-size: 1.75rem;
    margin-top: 2.5rem;
    margin-bottom: 1rem;
    padding-top: 1rem;
  }

  .project-content :global(h3) {
    font-size: 1.375rem;
    margin-top: 2rem;
    margin-bottom: 0.75rem;
  }

  .project-content :global(ul),
  .project-content :global(ol) {
    margin: 1.5rem 0;
    padding-left: 1.5rem;
  }

  .project-content :global(li) {
    margin-bottom: 0.5rem;
  }

  .project-content :global(blockquote) {
    border-left: 3px solid var(--accent);
    padding-left: 1.5rem;
    margin: 1.5rem 0;
    color: var(--text-secondary);
    font-style: italic;
  }

  .project-content :global(img),
  .project-content :global(figure) {
    max-width: 100%;
    height: auto;
    border-radius: 0.5rem;
    margin: 2rem 0;
  }

  .project-content :global(figcaption) {
    text-align: center;
    font-size: 0.875rem;
    color: var(--text-secondary);
    margin-top: 0.5rem;
  }

  /* Partner logo: white chip so the mark reads in both themes */
  .project-content :global(.fellowship-logo) {
    display: inline-block;
    background: #fff;
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.875rem 1.125rem;
    margin: 0.5rem 0 0;
    box-shadow: var(--shadow-sm);
  }

  .project-content :global(.fellowship-logo img) {
    display: block;
    width: 180px;
    height: auto;
    margin: 0;
    border-radius: 0;
  }

  .related {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 0.5rem;
    padding: 1.5rem;
    margin: 3rem 0;
  }

  .related h3 {
    font-size: 1.125rem;
    margin-bottom: 1rem;
  }

  .related ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .related li {
    margin-bottom: 0.5rem;
  }

  .project-nav {
    padding-top: 2rem;
    border-top: 1px solid var(--border);
  }

  .back-link {
    color: var(--accent);
    text-decoration: none;
    font-size: 0.9375rem;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .project-header h1 {
      font-size: 2rem;
    }

    .thumbnail {
      max-height: 250px;
    }

    .project-content {
      font-size: 1rem;
    }

    .project-content :global(h2) {
      font-size: 1.5rem;
    }

    .project-content :global(h3) {
      font-size: 1.25rem;
    }
  }
</style>
