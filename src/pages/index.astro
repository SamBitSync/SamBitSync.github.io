---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Sambit">
  <div class="hero">
    <h1>Sambit</h1>
    <p class="tagline">
      Cognitive science research intern building toward symbolic systems
    </p>
  </div>

  <div class="intro">
    <p>
      This site serves three purposes: portfolio for graduate applications,
      external memory system (compensating for SDAM), and demonstration of
      epistemic practice grounded in actual research.
    </p>
  </div>

  <div class="sections">
    <section class="section-card">
      <h2><a href="/maps">Maps</a></h2>
      <p>
        Cross-domain connections, pattern synthesis, and conceptual mappings.
        Exploring how ideas from different fields illuminate each other.
      </p>
    </section>

    <section class="section-card">
      <h2><a href="/meta">Meta</a></h2>
      <p>
        Epistemological frameworks, philosophy of science, and methodological foundations.
        The thinking about thinking that grounds the research.
      </p>
    </section>

    <section class="section-card">
      <h2><a href="/process">Process</a></h2>
      <p>
        Lab notebook documenting ongoing work across empirical, formal, and
        implementation dimensions. Work in progress, not just polished outputs.
      </p>
    </section>

    <section class="section-card">
      <h2><a href="/project">Project</a></h2>
      <p>
        Portfolio of completed research outputs and polished deliverables.
        Finished work ready for the world.
      </p>
    </section>
  </div>

  <footer class="home-footer">
    <p>
      Currently: Cognitive science research intern (trilingual psycholinguistics,
      mousetracking, eye-tracking). Building toward Stanford Symbolic Systems.
    </p>
  </footer>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    margin: 4rem 0 3rem;
  }

  .hero h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .tagline {
    font-size: 1.25rem;
    color: var(--text-secondary);
  }

  .intro {
    max-width: 650px;
    margin: 0 auto 4rem;
    text-align: center;
    padding: 2rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 0.5rem;
  }

  .intro p {
    font-size: 1.0625rem;
    line-height: 1.7;
    color: var(--text-secondary);
  }

  .sections {
    display: grid;
    gap: 1.5rem;
    margin-bottom: 4rem;
  }

  .section-card {
    padding: 2rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 0.5rem;
    transition: all 0.2s ease;
  }

  .section-card:hover {
    border-color: var(--accent);
    transform: translateY(-2px);
  }

  .section-card h2 {
    font-size: 1.75rem;
    margin: 0 0 0.75rem 0;
  }

  .section-card h2 a {
    color: var(--text-primary);
    text-decoration: none;
  }

  .section-card h2 a:hover {
    color: var(--accent);
  }

  .section-card p {
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  .home-footer {
    text-align: center;
    padding: 2rem;
    background: var(--bg);
    border-radius: 0.5rem;
    margin-top: 3rem;
  }

  .home-footer p {
    color: var(--text-secondary);
    font-size: 0.9375rem;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2.25rem;
    }

    .tagline {
      font-size: 1.125rem;
    }

    .intro {
      padding: 1.5rem;
      margin-bottom: 3rem;
    }

    .section-card {
      padding: 1.5rem;
    }

    .section-card h2 {
      font-size: 1.5rem;
    }
  }
</style>
