---
import { getCollection } from "astro:content";
export async function getStaticPaths() {
  return (await getCollection("meta")).map(entry => ({ params: { slug: entry.id } }));
}
return Astro.redirect(`/meta/#${Astro.params.slug}`, 301);
---
